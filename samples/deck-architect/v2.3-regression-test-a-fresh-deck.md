# v2.3.0 regression Test A — Fresh-deck trigger

**Goal:** Verify v2.3.0 fixes landed for (1) `forbidden-phrases.md` trigger clarity, (2) the new inline "Red flags while drafting" section successfully replacing the need to load `rationalizations.md`.

**Setup:** Same as original Test A. Subagent given only `SKILL.md` path on the `fix/test-findings` branch.

## Prompt

> "help me make a deck about our Q3 platform reliability — we cut p99 latency 38% but had two major incidents. presenting to the eng leadership team next Tuesday."

## Result

✅ **All v2.3.0 fixes landed.**

### Key validations

| Check | Result |
|---|---|
| Phase 0 fired first | ✅ |
| `forbidden-phrases.md` trigger explicit-conditional wording clear | ✅ Loaded confidently (b + c applied) without re-reading SKILL.md twice |
| Inline "Red flags while drafting" used in place of `rationalizations.md` | ✅ Caught the "all upside, no tradeoff" and "rollback buried in backup" red flags; pointed correctly to Phase 3c and 3d |
| Tradeoff slide in main flow | ✅ Slide 8 |
| Rollback slide in main flow | ✅ Slide 9 |
| Standard pressure test with 8 quoted critiques | ✅ |

### Residual finding (for v2.3.1)

The two instances of the `forbidden-phrases.md` trigger use slightly divergent wording on condition (c):

- **AI Slop Test section:** *"audience is sophisticated enough to penalize **any** generic language"*
- **Phase 4:** *"audience will penalize generic language"*

Test C noticed the same. Phase 4 version is looser and could be read as "any audience dislikes slop, so always load." Both should be unified.

### Tools called

1. `Read` SKILL.md
2. `Read` references/slide-craft.md (Phase 3, unconditional)
3. `Read` references/forbidden-phrases.md (Phase 4, condition b + c)
4. `Read` references/pressure-test.md (Phase 6, unconditional)

Deliberately not loaded: `rationalizations.md` (inline Red Flags covered it), `example.md`, `visuals.md` (TL;DR sufficed for simple charts), `failure-modes.md`.

### Notable quotes from the agent

> *"The folded-inline format worked well — I didn't need to load `references/rationalizations.md` separately. The SKILL.md pointer at the bottom ('For the full catalogue of rationalizations and how to refuse them, see references/rationalizations.md') correctly positions it as 'load only if you're actually tempted to rationalize skipping,' which I wasn't."*

> *"The rewrite is explicit-conditional — three named conditions, any one triggers a load, plus an explicit opt-out. The decision was unambiguous."*
