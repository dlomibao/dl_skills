# v2.3.0 regression Test C — Pitch + WebSearch

**Goal:** Verify v2.3.0 fixes landed for (1) WebSearch collection-page acknowledgment, (2) `forbidden-phrases.md` explicit-conditional trigger.

**Setup:** Same as original Test C — 10-min Sequoia seed pitch for AI-for-SRE with a hook image requirement.

## Result

✅ **Both fixes landed.**

### Key validations

| Check | Result |
|---|---|
| Pitch spine selected (Problem → Why Now → Solution → Why Us → Proof → Ask) | ✅ Quoted SKILL.md directly |
| Phase 0 fired with `[INFERRED — confirm]` flags | ✅ |
| `WebSearch` actually invoked (not just described) | ✅ Two parallel calls with specific queries |
| 3 candidate URLs surfaced with fit notes | ✅ iStock + Dreamstime + iStock |
| **WebSearch collection-page acknowledgment changed behavior** | ✅ Agent surfaced URLs honestly as collection pages with "pick the specific frame" note; explicitly said "I can't link the single image — the search doesn't return that depth." |
| Licensing flagged | ✅ Twice |
| **No fabricated URLs** | ✅ Agent: *"Every URL in my output came directly from WebSearch results. Where I didn't have a deep-link, I said so."* |
| Shoot-your-own recommendation surfaced | ✅ Strongly — "Take this photo yourself. Do not use stock." |
| `forbidden-phrases.md` trigger unambiguous | ✅ Loaded because conditions (b) and (c) both clearly applied |
| Standard pressure test with 8 quoted critiques | ✅ |

### Residual finding (for v2.3.1)

**Nice-to-have:** an inline URL example next to the collection-page acknowledgment would reduce mis-reads for first-time readers. Suggested wording:

> *"WebSearch typically returns stock-library collection pages (`https://www.istockphoto.com/photos/server-fire`) rather than direct image-asset URLs (`https://www.istockphoto.com/photo/server-on-fire-id12345.jpg`)."*

Agent understood without the example but flagged that the distinction would land faster on first read.

### Tools called

1. `Read` SKILL.md
2. `Read` references/slide-craft.md
3. `Read` references/forbidden-phrases.md
4. `Read` references/visuals.md (unconditional before non-text-only slide)
5. `Read` references/pressure-test.md
6. `WebSearch` "server rack on fire datacenter infrastructure chaos photo"
7. `WebSearch` "phone screen 3am pager alerts on-call engineer dark room photo"

### Notable quotes

> *"Without the v2.3.0 wording my training-data reflex would be to pretend one of those URLs was the image itself or to describe a specific stock image as if I'd seen it."*

> *"The decision was unambiguous. The Phase 4 repetition of the same trigger reinforced it."*
