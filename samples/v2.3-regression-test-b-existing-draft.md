# v2.3.0 regression Test B — Existing-draft path

**Goal:** Verify v2.3.0 fixes landed for (1) existing-draft guidance expansion, (2) "what changed and why" schema slot, (3) slide-count ceiling for external customer business review, (4) existing-draft takeaway exception.

**Setup:** Same as original Test B — 28-slide customer renewal review outline.

## Result

✅ **All 4 existing-draft fixes landed cleanly.** One cross-reference bug surfaced.

### Key validations

| Fix | Result | Evidence |
|---|---|---|
| Existing-draft guidance expansion (Phases 2/3/5–8 behavior) | ✅ Clear | Agent quoted the new text and followed it without needing to re-interpret |
| "What changed and why" schema slot | ✅ Used | 8 entries populating the slot (promoted recommendation, collapsed topic sections, reframed titles, cut opener, added tradeoff, promoted rollback, replaced close, added backup layer) |
| External customer business review ceiling (8–12 + appendix) | ✅ Used | Agent cited the exact row and justified the 28→10 cut with "2.3x the ceiling" |
| Existing-draft takeaway exception | ✅ Followed exactly | Marked takeaway as `[USER: write the one-sentence takeaway]`, flagged as blocking in handoff notes, reinforced in final "What I need from you" list |

### Residual finding (for v2.3.1)

**Cross-reference error:** The new existing-draft guidance says *"the Phase 9 schema has a slot for this"* — but the schema is actually in Phase 8. In v2.2.0 I renumbered Phase 10 → 9 (handoff) and Phase 9 → 8 (present the outline). The v2.3.0 text didn't catch the shift. Substance is correct; reference number is wrong.

### Tools called

1. `Read` SKILL.md
2. `Read` references/slide-craft.md
3. `Read` references/forbidden-phrases.md (all 3 conditions fired — existing draft had "Key Themes")
4. `Read` references/pressure-test.md

### Notable quote

> *"The `[USER: write]` template + blocking-flag pattern is exactly right. I'd have been stuck without it."*
