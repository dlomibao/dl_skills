# Output format

The Phase 9 output schema lives inline in `SKILL.md` (it's used on every run, so splitting it out wastes a load). This file exists only as a redirect — see Phase 9 in `SKILL.md` for the canonical template.
